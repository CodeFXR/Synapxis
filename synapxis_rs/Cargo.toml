[package]
name = "synapxis_rs"
version = "0.1.0"
edition = "2024"

[lib]
name = "synapxis_rs"
crate-type = ["cdylib"]

[dependencies]
# Change this line to include the feature, but keep the new version
pyo3 = { version = "0.27.0", features = ["extension-module"] }
